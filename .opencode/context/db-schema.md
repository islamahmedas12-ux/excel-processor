# Database Schema — excel-processor

This project is **stateless** — no database, no storage.
Every request is independent. Files are processed in-memory and not saved.

See api-contracts.md for data structures.
